Steps:

//Hello Express App.

install express globaly by: npm install -g express 
for checking express is installed successfully check by: npm express --version

now goto your app folder, and install dependency too by: npm install express --save

for further check app.js

For run application goto nodejs command prompt type: node app.js


Now installing EJS Templating Node Module

type: npm install ejs --save

set view engine in app.js and add some pages